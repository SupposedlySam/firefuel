rules_version = '2';

// The live test project exists only for firefuel's integration suite. Tests sign
// in anonymously; nothing is readable or writable without a signed-in user.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
