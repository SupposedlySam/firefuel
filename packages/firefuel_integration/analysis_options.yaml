include: package:very_good_analysis/analysis_options.yaml

analyzer:
  errors:
    public_member_api_docs: ignore
    # Test files are imported from packages/firefuel by relative path.
    avoid_relative_lib_imports: ignore
  exclude:
    - build/**
    - macos/**
    - ios/**
